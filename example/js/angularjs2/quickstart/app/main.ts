import { bootstrap }    from '@angular/platform-browser-dynamic';
import { enableProdMode } from '@angular/core';  // optional
import { AppComponent } from './app.component2';

enableProdMode(); // optional
bootstrap(AppComponent);